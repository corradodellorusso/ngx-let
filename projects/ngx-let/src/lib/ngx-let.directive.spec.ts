import { NgxLetDirective } from './ngx-let.directive';

describe('NgxLetDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxLetDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
